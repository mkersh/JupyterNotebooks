(ns practicalli.simple-api-server-test
  (:require [clojure.test :refer :all]
            [practicalli.simple-api-server :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
